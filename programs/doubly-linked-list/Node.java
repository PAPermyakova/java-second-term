public class Node {

    public int element; // данные узла
    public Node next; // указатель на след элемент
    public Node prev; // указатель на предыдущ элемент

    public Node(int element) { // конструктор
        this.element = element;
    }
}
